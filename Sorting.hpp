#pragma once

#include <vector>

extern void bubbleSort(std::vector<int>& data);
extern void selectionSort(std::vector<int>& data);
extern void insertionSort(std::vector<int>& data);

extern std::vector<int> mergeSort(std::vector<int> array);
